# Changelog

## [Unreleased]

### Added

- Initial release of the `anta-agent` CLI: the coding agent with the built-in four-layer memory engine enabled, using `~/.anta-agent` as its independent data directory. Enter with the `at` command.
